import SwiftUI

// Legacy ContentView - keeping for compatibility
// The app now uses AppRootView from iOSApp.swift
struct ContentView: View {
    var body: some View {
        AppRootView()
    }
}

struct ContentView_Previews: PreviewProvider {
    static var previews: some View {
        ContentView()
    }
}
